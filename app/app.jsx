const React = require('react');
const ReactDOM = require('react-dom');

/* Import Components */
const Problems = require('./components/Problems');

ReactDOM.render(<Problems />, document.getElementById('main'));